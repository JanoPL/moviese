<form method="post" action="{{ path('app_movie_se_delete', {'id': entertaiment_product.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ entertaiment_product.id) }}">
    <button class="btn">Delete</button>
</form>
