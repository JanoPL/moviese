{% extends 'base.html.twig' %}

{% block title %}EntertaimentProduct{% endblock %}

{% block body %}
    <h1>EntertainmentProduct</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entertaiment_product.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_movie_se_index') }}">back to list</a>

    <a href="{{ path('app_movie_se_edit', {'id': entertaiment_product.id}) }}">edit</a>

    {{ include('movie_se/_delete_form.html.twig') }}
{% endblock %}
