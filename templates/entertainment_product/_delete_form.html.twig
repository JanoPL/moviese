<form method="post" action="{{ path('app_entertainment_product_delete', {'id': entertainment_product.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ entertainment_product.id) }}">
    <button class="btn btn-danger">Delete</button>
</form>
